@* major/index.cshtml *@

@if (Model.Count != 0)
{
  <ul>

  @foreach(var major in Model)
  {
    <li><a href="/major/@major.Id">@major.Name</a></li>
  }
  </ul>
}

<a asp-controller="major" asp-action="new">Add a new major</a>
