
@if (Model.Count != 0)
{
  <ul>

  @foreach(var course in Model)
  {
    <li><a href="/course/@course.Id">@course.Name</a></li>
  }
  </ul>
}

<a asp-controller="course" asp-action="new">Add a new course</a>
